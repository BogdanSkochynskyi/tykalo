# Test defaults for every @SpringBootTest context.
# A non-blank token satisfies TelegramBotProperties' fail-fast check; polling is
# disabled so no test ever opens a long-polling connection to Telegram.
telegram.bot.token=test-token
telegram.bot.polling.enabled=false
